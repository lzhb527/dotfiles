
1. 安装与目录准备
首先安装 MPD 并在你的家目录下创建必要的数据库和列表文件
（以用户模式运行，这是 openSUSE 桌面用户的最佳实践）

sudo zypper install mpd
mkdir -p ~/.config/mpd/playlists

2. 编辑配置文件
创建或编辑 ~/.config/mpd/mpd.conf。在 openSUSE 中，
建议使用以下精简且有效的配置：



# 基础路径设置
music_directory    "~/Music"
playlist_directory "~/.config/mpd/playlists"
db_file            "~/.config/mpd/database"
log_file           "~/.config/mpd/log"
pid_file           "~/.config/mpd/pid"
state_file         "~/.config/mpd/state"
sticker_file       "~/.config/mpd/sticker.sql"

# 网络监听 (仅限本地访问)
bind_to_address    "127.0.0.1"
port               "6600"

# 自动更新数据库
auto_update        "yes"

# 音频输出配置 (openSUSE 默认使用 PipeWire 或 PulseAudio)
audio_output {
        type            "pulse"
        name            "PulseAudio/PipeWire"
}

# 可选：为 ncmpcpp 等提供频谱可视化支持
audio_output {
    type                    "fifo"
    name                    "my_fifo"
    path                    "/tmp/mpd.fifo"
    format                  "44100:16:2"
}


3. 处理权限与服务启动
openSUSE 默认可能启用了系统级的 MPD 服务，这会导致冲突。
我们需要禁用系统级，启用用户级：


# 停止并禁用系统全局服务
sudo systemctl disable --now mpd.service

# 启动并启用当前用户的 MPD 服务
systemctl --user enable --now mpd.service






