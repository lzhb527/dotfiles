# 设置高度为屏幕的 30% 或指定行数
lines 18
columns 40

# 设置屏幕边缘位置（top, bottom, left, right, center）
edge top
margin_left 10
margin_right 10

# 背景透明度
background_opacity 0.98

# 自动隐藏设置：当失去焦点时自动隐藏
hide_on_focus_loss yes

# 使用 kitty_override 覆盖字体大小，而不影响主终端
kitty_override font_size=13
