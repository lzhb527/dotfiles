-- =========================================================================
-- tiny-inline-diagnostic.nvim 光标行内诊断配置
-- =========================================================================
return function()
	require("tiny-inline-diagnostic").setup({
		-- 插件本身的个性化配置可以写在这里，默认就非常好看
		signs = {
			left = "▍",
			right = " ",
		},
	})
end
